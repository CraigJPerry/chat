dependencies:
    pre:
        - npm install -g typescript
        - npm install -g typings
        - typings install --save

test:
    post:
        - mkdir -p $CIRCLE_TEST_REPORTS/mocha/
        - mv test-results.xml $CIRCLE_TEST_REPORTS/mocha/

deployment:
  prod:
    branch: heroku-onboarding
    heroku:
      appname: craigjperry-chat
